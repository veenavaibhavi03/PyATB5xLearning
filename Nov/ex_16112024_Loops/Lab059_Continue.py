for num in range(10):
    if num % 2 == 0:
        continue # it will execute and goes back to the iteration
    else:
        print(num)

# pass can be used in the statements, functions, classes and objects.